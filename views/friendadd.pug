extends base.pug

block content
    form(method="POST")
        input(placeholder="Email", name="email" type="text", required)
        input(type="submit", value="Update")

        span.friend_component
    include partials/footer.pug