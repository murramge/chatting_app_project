extends base.pug
block body
    body#index-background
    block content
        div.welcome-header
            h1.welcome-header__title Welcome to
            h2.welcome-header__title2 🍒 Chat Talk 🐹
            p.welcome-header__text 계정이 있으면 이메일로 로그인 해주세요
            if errorMessage
                span.errormessage=errorMessage 
        form(method="POST")#login-form
            input(placeholder="Email", name="email", type="text", required)
            input(placeholder="password",name="password", type="password", required) 
            input(type="submit", value="Login")
            a(href="/join").joininto 아직 계정이 없으신가요? → JOIN
